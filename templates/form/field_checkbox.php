<?php defined('ABSPATH') || exit; ?>

<div class="{{ class }}" data-field="{{ field_name }}">
    {{ label }}
    <div class="glsr-field-subgroup">
        {{ field }}
    </div>
    {{ errors }}
</div>
