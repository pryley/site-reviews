<?php defined('WPINC') || die; ?>

<div class="glsr-review">
    {{ title }}
    {{ rating }}
    {{ date }}
    {{ assigned_links }}
    {{ content }}
    {{ avatar }}
    {{ author }}
    {{ response }}
</div>
