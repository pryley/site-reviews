<?php defined('WPINC') || die; ?>

<div class="glsr-review" id="{{ review_id }}">
    {{ title }}
    {{ rating }}
    {{ date }}
    {{ assigned_links }}
    {{ content }}
    {{ avatar }}
    {{ author }}
    {{ response }}
</div>
