<?php defined('ABSPATH') || die; ?>

<div class="glsr-review" id="review-{{ review_id }}">
    {{ title }}
    {{ rating }}
    {{ date }}
    {{ assigned_links }}
    {{ content }}
    {{ avatar }}
    {{ author }}
    {{ response }}
</div>
