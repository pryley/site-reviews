<?php defined( 'WPINC' ) || die; ?>

<div class="glsr-reviews-wrap" data-assigned="{{ assigned_to }}" data-category="{{ category }}">
	<div class="{{ class }}" id="{{ id }}">
		{{ reviews }}
		{{ navigation }}
	</div>
</div>
