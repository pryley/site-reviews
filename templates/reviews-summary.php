<?php defined( 'WPINC' ) || die; ?>

<div class="glsr-summary-wrap" data-assigned="{{ assigned_to }}">
	<div class="{{ class }}" id="{{ id }}">
		{{ rating }}
		{{ stars }}
		{{ text }}
		{{ percentages }}
	</div>
</div>
