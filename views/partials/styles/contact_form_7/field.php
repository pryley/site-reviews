<?php defined('WPINC') || die; ?>

<p class="{{ class }}">
    <label for="{{ for }}">{{ label_text }}<br>
        <span class="wpcf7-form-control-wrap">
            {{ field }}
            {{ errors }}
        </span>
    </label>
</p>
