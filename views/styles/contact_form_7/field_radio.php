<?php defined('ABSPATH') || exit; ?>

<p class="{{ class }}" data-field="{{ field_name }}">
    {{ label }}
    <span class="glsr-field-subgroup">
        {{ field }}
    </span>
    {{ errors }}
</p>
