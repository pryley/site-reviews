<?php defined('ABSPATH') || exit; ?>

<p class="glsr-heading">fallback</p>
<p>Include the "fallback" option to change the text that is shown when the review is not found. This option will override the default fallback text.</p>
<p>The default fallback value is: <code>"Review not found."</code></p>
<div class="shortcode-example">
    <input type="text" readonly class="code" value='[site_review fallback="Invalid review."]'>
    <pre><code class="syntax-shortcode"><span class="tag">[site_review</span> <span class="attr-name">fallback</span>=<span class="attr-value">"Invalid review."</span><span class="tag">]</span></code></pre>
</div>
