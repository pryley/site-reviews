<?php
/**
 * Plugin Name: Site Reviews: Plain Addon
 * Plugin URI:  https://niftyplugins.com/plugin/site-reviews-plain-addon/
 * Version:     1.0.0
 * Domain Path: /languages
 * Tested up to: 6.9
 * GLSR requires at least: 8.0.0
 * GLSR unsupported version: 99.0.0
 *
 * A FIXTURE, not a plugin — the plainest addon possible: no post type (about
 * half the real addons register none) and no plugin/Integrations directory. It
 * exists for the addon framework's "this addon does not do that" branches,
 * which the test addon — which does everything — can never reach.
 *
 * Never loaded by WordPress. Plugin::__construct() derives $this->file by
 * swapping `plugin/Application` for the addon's id, so the LAYOUT matters:
 * this file sits next to a plugin/ directory and is named after the id.
 */
